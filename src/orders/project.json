{
  "name": "orders",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "src/orders",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd src/orders && ./mvnw package"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd src/orders && ./mvnw test -DexcludedGroups=integration"
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd src/orders && ./mvnw test -Dgroups=integration"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd src/orders && ./mvnw spring-boot:run"
      }
    },
    "container": {
      "executor": "@nx-tools/nx-container:bordersld",
      "options": {
        "context": "src/orders",
        "metadata": {
          "images": ["public.ecr.aws/aws-containers/retail-store-sample-orders"]
        }
      }
    }
  },
  "tags": ["service"]
}
