{
  "name": "checkout",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd src/checkout && yarn install"
      }
    },
    "test:e2e": {
      "dependsOn": ["install"]
    }
  },
  "tags": ["service"]
}
