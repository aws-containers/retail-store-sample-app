name: Checkout CI

on:
  push:
    branches:
      - main
    paths:
      - "src/checkout/**"
  pull_request:
    branches:
      - main
    paths:
      - "src/checkout/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-module:
    uses: ./.github/workflows/shared-ci.yml
    with:
      component: checkout
    secrets: inherit
