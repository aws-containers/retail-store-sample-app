name: Catalog CI

on:
  push:
    branches:
      - main
    paths:
      - "src/catalog/**"
  pull_request:
    branches:
      - main
    paths:
      - "src/catalog/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-module:
    uses: ./.github/workflows/shared-ci.yml
    with:
      component: catalog
    secrets: inherit
