name: Orders CI

on:
  push:
    branches:
      - main
    paths:
      - "src/orders/**"
  pull_request:
    branches:
      - main
    paths:
      - "src/orders/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-module:
    uses: ./.github/workflows/shared-ci.yml
    with:
      component: orders
    secrets: inherit
